function runTypingEffect() {
  const text = 'I am Johnny "Domestico" Trinh.';
  const typingElement = document.getElementById('typing-text');
  const typingDelay = 100;

  typeText(text, typingElement, typingDelay);
}
